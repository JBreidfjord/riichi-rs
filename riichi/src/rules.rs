
/// TODO(summivox): rules

#[derive(Clone, Debug)]
pub struct Rules();

impl Default for Rules {
    fn default() -> Self {
        // TODO(summivox): rules: default values should agree with Tenhou/Majsoul
        Self()
    }
}
